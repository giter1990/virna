import React, { Component} from "react";
import { hot } from "react-hot-loader";
import "./App.css";

// class App extends Component{
	// render(){
		// const user = {
				// firstName: 'Harper',
				// lastName: 'Perez',
				// avatarUrl: "stone.jpg"
			// },
			// element = (
				// <div>
					// <h1>Hello!</h1>
					// <h2>Good to see you here.</h2>
				// </div>
			// ),
			// title = prompt("Enter"),
			// elementEnter = (<h1>{title}</h1>);
		
		// function formatName(user) {
			// return user.firstName + ' ' + user.lastName;
		// }
		
		// function getGreeting(person) {
			// if (person) {
				// return <span>Hello, {formatName(person)}!</span>;
			// }
			
			// return <h1>Hello, Stranger.</h1>;
		// }
		
		// console.log(typeof(title));
		
		// return(
			// <div className="App">
				// <h1 tabIndex="0" style={{color: "blue"}}>Привет, {formatName(user)}</h1>
				// <img src={user.avatarUrl} />
				// <p>Call result: {getGreeting(user)}</p>
				// <div>{element}</div>
				// <div>{elementEnter}</div>
			// </div>
		// )
	// }
// }

// let template = require("./pug/index.pug");

// let html = template();

// export default hot(module)(App);